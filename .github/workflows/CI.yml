# This is a basic workflow to help you get started with Actions

name: CI
env:
  APPLICATION_NAME : "MyApps"
  PACKAGE_NAME     : "MyApps-ver-${{ github.sha }}"
  #CREDLY_USER      : "maksim-rusetski"
  CREDLY_USER      : "Maksim Rusetski"
  

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches:    
      - 'main'
      - 'learn/**'
    paths-ignore:
      - '**/README.md'

  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    env: 
      BUILD_VAR_1: "job level variable"
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.

      - name: Print variables
        env: 
          LOCAL_VAR: "Super local var"
        run: |
          echo "BUILD_VAR_1 is ${{ env.BUILD_VAR_1 }}" 
          echo "BUILD_VAR_1 is $LOCAL_VAR" 
          
  my_deploy:
    runs-on: ubuntu-20.04
    needs: [ build ]
    steps:
      - name: List current folder befor Checkout
        run : ls -la

      - name: Checkout
        uses: actions/checkout@v3
      
      - name: List current folder after Checkout
        run : ls -la

      - name: Print Hello world
        run : echo "Hello world from my_deploy"

      - name: Print variables
        run: |
          echo "Application name is ${{ env.APPLICATION_NAME }}" 
          echo "Package name is ${{ env.PACKAGE_NAME }}"
      
      #- name: Badges - Readme
      #  # You may pin to the exact commit or the version.
      #  # uses: pemtajo/badge-readme@3836d7866ca0765b3b4ff8f0f826037657a5f434
      #  uses: pemtajo/badge-readme@2.3.0
      #  with:
      #    # GitHub access token with Repo scope # default is ${{ github.token }}
      #    GH_TOKEN: ${{ secrets.GH_TOKEN }}
      #    # Your GitHub repository # optional, default is ${{ github.repository }}
      #    REPOSITORY: ${{ github.repository }}
      #    # Add a commit message of your choice
      #    #COMMIT_MESSAGE: # optional, default is Updated README with new badges
      #    # User name used in Credly # optional, default is ${{ github.actor }}
      #    CREDLY_USER: ${{ env.CREDLY_USER }}
      #    # The sort type for return credly badges [RECENT/POPULAR] # optional, default is 
      #    CREDLY_SORT: RECENT
      #    # Defines the badge dimension.# optional, default is 110
      #    BADGE_SIZE: 110
      #    # the number of the last badges that need to show - (0 to not set limit) # optional, default is 0
      #    NUMBER_LAST_BADGES: 0
      - name: Badges - Readme
        uses: pemtajo/badge-readme@main
        with:
          CREDLY_USER: ${{ env.CREDLY_USER }}